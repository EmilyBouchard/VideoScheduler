<Window x:Class="VideoScheduler.Presentation.WPF.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:VideoScheduler.Presentation.WPF.Views"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" />

        <Border Grid.Row="1" Margin="0,12,0,0" Padding="16" CornerRadius="12">
            <StackPanel>
                <TextBlock Text="Scaffold is ruynning (Toolkit MVVM + Generic Host + DI)." />
                <TextBlock Margin="0,8,0,0" Opacity="0.75" Text="Next: add VideoLibrary module + navigation host + templates." />
            </StackPanel>
        </Border>
    </Grid>
</Window>