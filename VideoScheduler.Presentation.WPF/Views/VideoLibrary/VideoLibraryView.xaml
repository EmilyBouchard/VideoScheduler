<UserControl x:Class="VideoScheduler.Presentation.WPF.Views.VideoLibrary.VideoLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Margin="12" Grid.Row="0" LastChildFill="True">
            <TextBlock Text="Root Folder:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Text="{Binding RootFolder, UpdateSourceTrigger=PropertyChanged}" MinWidth="360" />
            <Button Content="Scan" Margin="8,0,0,0"
                    Command="{Binding LoadAsyncCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource BooleanInverterConverter}, FallbackValue=True}" />
        </DockPanel>

        <Grid Grid.Row="1" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="Folders" Grid.Column="0">
                <ListBox ItemsSource="{Binding Folders}"
                         SelectedItem="{Binding SelectedFolder}"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         ScrollViewer.CanContentScroll="True">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>

            <GroupBox Header="Videos" Grid.Column="2">
                <ListView ItemsSource="{Binding Videos}"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          ScrollViewer.CanContentScroll="True">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="File" DisplayMemberBinding="{Binding FileName}" Width="360" />
                            <GridViewColumn Header="Duration" DisplayMemberBinding="{Binding DurationText}" Width="120" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>
        </Grid>

        <StatusBar Grid.Row="2">
            <StatusBarItem Content="{Binding StatusText}" />
            <Separator />
            <StatusBarItem Content="{Binding IsLoading, StringFormat=Loading: {0}}" />
        </StatusBar>
    </Grid>
</UserControl>
